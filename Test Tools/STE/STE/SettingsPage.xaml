<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="STE.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:STE"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" Spacing="10" Padding="10">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Spacing="10">
            <Button Width="100" Height="40" Click="OpenHomePage">Home</Button>
        </StackPanel>

        <StackPanel Orientation="Vertical" Spacing="4" HorizontalAlignment="Left">
            <TextBlock Text="Startup delay (seconds) before test cases run"/>
            <TextBox x:Name="StartupDelayTextBox" Width="300" TextChanged="StartupDelayTextBox_TextChanged"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" Spacing="4" HorizontalAlignment="Left">
            <TextBlock Text="Programs started by the Launch Programs button"/>
            <ListBox ItemsSource="{Binding Programs}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Content="{Binding Name}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </StackPanel>
</Page>
